# Archive — frozen, do not use

One-time setup and superseded config. **Active** rules and skills live in
`.cursor/rules/` and `.cursor/skills/`.

**Agents:** do not read, invoke, or suggest anything under `.cursor/_archive/`
unless the user explicitly asks to restore from archive.

| Archived | Active replacement |
|----------|-------------------|
| `skills/find-your-level/` | `progress/learning-profile.json`, README phase tables |
| (future) | — |

Placement and learning preferences belong in `progress/` after setup.
